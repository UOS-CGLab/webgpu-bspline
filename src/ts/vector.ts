export default class Vector {
  x: number;
  y: number;

  /**
   * 2차원 좌표를 표현하는 클래스
   * @param x x좌표
   * @param y y좌표
   */
  constructor(x: number, y: number) {
    this.x = x;
    this.y = y;
  }
}
